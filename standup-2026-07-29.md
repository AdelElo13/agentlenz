# Standup — 2026-07-29

## Done (last 24h)

### agentlenz
- `3f11db0` standup: 2026-07-28 _(automated daily standup)_

### dockwright-macos-agent
_No commits pushed in the last 24 hours._

## Today's Calendar

_No events scheduled for today._

## In Progress

### agentlenz
- Automated standup & weekly health report pipeline running on schedule
- No open feature branches — all work is on `main`

### dockwright-macos-agent
- No active development — last commit `d6d3f3f chore: update UIAutomationTool` (~115 days ago)
- No open feature branches

## Action Items

### Overdue (5+ weeks — action needed now)
- [ ] **agentlenz**: Remove tracked `.pyc` / `__pycache__` from git index — one command:
  ```bash
  git rm -r --cached $(git ls-files | grep -E '\.pyc$|__pycache__')
  git commit -m "chore: remove tracked pyc files from index"
  git push
  ```
- [ ] **agentlenz dashboard**: Run `npm audit fix --force` in `dashboard/` — HIGH-severity Next.js CVEs (DoS, proxy bypass, cache poisoning) outstanding 3+ weeks
- [ ] **agentlenz**: Add backend lockfile (`uv.lock`) — flagged 5 consecutive weeks; builds non-reproducible

### High priority
- [ ] **agentlenz**: Fix teardown `RuntimeError` in `EventClient.flush()` — guard with `if not self._config: return`
- [ ] **agentlenz**: Wire pytest to GitHub Actions CI so test pass/fail is visible per push
- [ ] **dockwright**: Confirm active development status — 115 days without a commit; archive on GitHub if inactive

### Short-term
- [ ] **agentlenz**: Add dashboard test suite (Vitest or Playwright)
- [ ] **dockwright**: Migrate `.onnx` models and `demo.mov` to Git LFS or external hosting
- [ ] **dockwright**: Add Swift test target covering core logic (`CronEngine`, `LLMService`, `ToolExecutor`)
